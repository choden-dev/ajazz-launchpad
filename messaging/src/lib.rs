/// Module which contains the generated rust code from the protobuf messages

// Note: only the `protos` module is allowed to use the old `mod.rs` system as the codegen only allows that format
pub mod protos;
